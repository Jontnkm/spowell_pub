<%
var depth2 = locals.depth2 ? locals.depth2 : 1;
var depth3 = locals.depth3 ? locals.depth3 : 0;
var url = locals.url ? locals.url : '../../';
var depth2P = [];
var depth3P = [];
for(var i=1;i<=6;i++){
    depth3P[i] = [];
    if(depth2==i){
        depth2P[i] = ' active';
        for(var j=1;j<=6;j++){
            if(depth3==j){
                depth3P[i][j] = ' active';
            }else{
                depth3P[i][j] = '';
            }
        }
    }else{ 
        depth2P[i] = "";
        for(var j=1;j<=6;j++){
            depth3P[i][j] = '';
        }
    }
}
%>
<div class="cptLnb">
    <div class="lnbArea">
        <h2 class="h2title">나의 대출조회</h2>
        <ul class="depth2Group">
            <li class="depth2Menu<%= depth2P[1] %>">
                <h3 class="depth2Name"><a href="<%= url %>html/mypage/loan_status.html" class="depth2Link">대출심사현황</a></h3>
            </li>
            <li class="depth2Menu<%= depth2P[2] %>">
                <h3 class="depth2Name"><a href="<%= url %>html/mypage/loan_info.html" class="depth2Link">대출정보 조회</a></h3>
            </li>
            <li class="depth2Menu<%= depth2P[3] %>">
                <h3 class="depth2Name"><a href="<%= url %>html/mypage/loan_doc.html" class="depth2Link">각종서류 제출</a></h3>
            </li>
            <li class="depth2Menu<%= depth2P[4] %>">
                <h3 class="depth2Name"><a href="<%= url %>html/mypage/loan_confirm.html" class="depth2Link">고객거래확인(재이행) 안내</a></h3>
            </li>
            <li class="depth2Menu<%= depth2P[5] %>">
                <h3 class="depth2Name"><a href="<%= url %>html/mypage/loan_voice.html" class="depth2Link">음성녹음 내용 확인</a></h3>
            </li>
        </ul>
    </div>
</div>